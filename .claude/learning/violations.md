# Violations Ledger — Pattern-Adoption Telemetry

**Append-only.** Every Critical/High finding surfaced during the babysit loop, `/check`, or `/code-review` is logged here as one line. Format and rules: `.claude/learning/protocol.md` § Pattern-Adoption Telemetry.

The ledger feeds the **recurring-violations surfacing** pass: codes that appear ≥5 times in 30 days are candidates for promotion into a hook, into BASELINE, or into a module-specific knowledge entry.

**Never edit historical entries.** Mark promoted ones with `[PROMOTED YYYY-MM-DD]` only.

---

## Entries

| Date | Code | Fixed-by | Context |
| ---- | ---- | -------- | ------- |
| 2026-07-17 | B-H-reconnect-unref | finish-task /code-review | session-manager reconnect setTimeout not unref'd — up to RECONNECT_MAX_DELAY_MS SIGTERM shutdown delay when gateway enabled; fixed with .unref(). |
| 2026-07-17 | F-C3 | finish-task /code-review | Unused amber palette exported from web theme colors.ts — latent yellow/amber supply-side surface; removed. |
| 2026-07-17 | B-C8 | accepted | GET /api/health returns {ok,version,uptimeSeconds} not {ok,data} — pre-existing monitoring-probe shape read via .ok; accepted, follow-up if web consumes it. |
| 2026-07-17 | B-C1 (design) | code-reviewer | device.entity.ts `toJSON()` still includes `webhookUrl` field from the legacy single-URL design; spec §5.3 replaces it with 5 per-event columns in PR2, but the field lingers in the PR1 entity and schema — latent contract drift. |
| 2026-07-17 | SC-H1 | code-reviewer | AC-5 webhook-event routing (dispatch-webhook) not updated in PR1 despite spec §9.1 listing it as a PR1 test target; `DispatchWebhookUseCase` still uses `device.webhookUrl` single-URL, not the per-event columns — deferred but not explicitly documented in spec Out list. |
| 2026-07-17 | B-H7 (design) | code-reviewer | `prisma-user-repository.ts` admin `create` path opens an inline $transaction to provision account+user, creating a second code path for account creation parallel to `executeSignUpTransaction`; DRY concern — should reuse the same transaction function. |
| 2026-07-17 | proposed (B-M-stale-QR-window) | code-reviewer | GetDeviceQrUseCase: status=QR_PENDING read and gateway.getCurrentQr() are two separate reads — a QR rotation can deliver a new QR before the DB status write arrives, producing a brief window where qr=null even while QR_PENDING is true; low-probability but real race. |
| 2026-07-17 | SC-H1 | code-reviewer | Spec §9.1 requires update-device-webhooks spec to assert webhook_secret is untouched; the existing spec only asserts `not.toHaveProperty("webhookSecret")` on the JSON projection, not that the stored secret value is unchanged after an update. |
| 2026-07-17 | not-in-checklist (propose: B-H-cache-eviction-race) | code-reviewer | cached-devices.repository.ts write-then-evict ordering leaves a sub-TTL stale window for concurrent findByIdInternal (webhook dispatch); stale webhook URLs for up to CACHE_ENTITY_TTL_SECONDS; env comment does not document the write-race window. |
| 2026-07-17 | not-in-checklist (propose: B-H-di-per-call-resolve) | code-reviewer | api-token-auth.middleware.ts resolves ICacheProvider and AppConfig via container.resolve() on every request inside the closure instead of capturing singletons at middleware creation time. |
| 2026-07-17 | B-C2 (Critical) | code-reviewer | prisma-user-repository.ts findById does findUnique without deleted_at:null — soft-deleted user returned as live; cached-user-repository.ts inherits and extends the window to CACHE_ENTITY_TTL_SECONDS; auth middleware does not check deletedAt/status after findById. |
| 2026-07-17 | not-in-checklist (propose: B-H-di-per-call-resolve) | code-reviewer | api-token-auth.middleware.ts recurring: container.resolve for all 4 singletons inside request handler — same pattern as violations.md 2026-07-17 entry; auth.middleware.ts has identical pattern for JwtProvider and UserRepository. |
| 2026-07-17 | not-in-checklist (propose: F-H-no-confirm-on-first-generate) | code-reviewer | ApiTokenTab EmptyState onAction wires directly to handleGenerate — first-ever token generation fires the mutation immediately with no confirmation; only re-generation has a ConfirmDialog; UX asymmetry and no harm guard for misclick. |
| 2026-07-17 | not-in-checklist (propose: F-H-qr-connect-mutation-in-dep-array) | code-reviewer | QrConnectModal useEffect dep array lists `connectDevice` (the useMutation return object); TanStack Query v5 makes `mutate` stable but the mutation *object* recreates on every render — dep should be `connectDevice.mutate` to avoid potential extra connect calls on fast re-renders. |
| 2026-07-17 | SC-H1 | code-reviewer | AC-9 requires unit tests for the devices module (hooks, components with QR polling lifecycle, create flow, webhook autosave); no *.spec.ts exists under apps/web/src/modules/devices or apps/web/src/modules/account; queryKeys.spec.ts does not cover the new devices/account namespaces. |
| 2026-07-17 | proposed (F-H-trapped-skeleton) | duck-challenger | QrConnectModal: connectStartedRef blocked retry after connectMutate failure; QR skeleton rendered indefinitely with only a toast, no in-modal recovery. Fixed: onError resets the ref, stops the poll, renders an error state + retry button (qr.error/qr.retry i18n ×3). |
| 2026-07-17 | not-in-checklist (propose: F-M-sandbox-dead-type-select) | code-reviewer | SandboxPage messageType is bound in form state and rendered in SelectField but never read in handleSend — always calls sendText regardless of selection; dead/confusing state masquerading as a real option. |
| 2026-07-17 | not-in-checklist (propose: F-M-stale-closure-device-select) | code-reviewer | SandboxPage useEffect reads form.formData.deviceId inside a closure that only re-creates when connectedDevices changes; formData.deviceId is stale if user changed the select after the last connectedDevices update, causing spurious device-select resets on background refresh. |
| 2026-07-17 | proposed (B-H-gateway-logout-unguarded) | duck-challenger | DisconnectDeviceUseCase.execute() calls gateway.logout() which hits bare sock.logout() in session-manager with no try/catch — new use case inherited pre-existing unguarded path from delete-device; sock maps already cleaned before call, wrap is safe. |
| 2026-07-17 | proposed (B-H-bare-gateway-logout) | duck-challenger | session-manager.logout awaited sock.logout() with no try/catch; a Baileys throw on a mid-handshake (QR_PENDING) socket would bubble to an unhandled 500. The new status-agnostic POST /devices/:id/disconnect made that path reachable via direct API. Fixed: wrapped in try/catch, logs and swallows (in-process state already cleared). Also hardens the shared delete-device path. |
| 2026-07-17 | proposed (F-M-crypto-randomuuid-no-fallback) | duck-challenger | HttpMessagingRepository used crypto.randomUUID() with no guard — throws TypeError on non-secure-context (plain-HTTP non-localhost) dev origins. Fixed: newIdempotencyKey() falls back to a non-crypto unique string when crypto.randomUUID is unavailable. |
| 2026-07-18 | not-in-checklist (propose: F-M-shadow-token-rename-drift) | code-reviewer | semantic-tokens.ts renamed shadow tokens to camelCase (shadow.cardHover) but two pre-existing consumers PhoneInputField.tsx:336 and CountrySelectField.tsx:259 still reference "card-hover" (hyphen) — silent visual regression on hover, not caught by build/types. |
| 2026-07-18 | not-in-checklist (propose: F-L-global-style-rgba-drift) | code-reviewer | theme/index.ts global body backgroundImage still uses old cold rgba(70,78,88) / rgba(179,186,194) from before the warm-palette task; very faint (0.04-0.05 alpha) but now semantically stale against the warm brand. |
| 2026-07-18 | F-C3 (scoped exception) | code-reviewer | accent.gold dark value #FACC15 in semantic-tokens.ts is Tailwind yellow-400; documented as intentional product exception for star/favorite affordance, no current consumer, but introduces supply-side yellow against the hard no-yellow rule. |
| 2026-07-18 | not-in-checklist (propose: F-M-nav-item-duplication) | code-reviewer | MobileBottomNav.tsx defines its own bottomNavItems array (4 items) in parallel to navigation.ts; adding a 5th nav item requires editing both files — DRY violation that will cause sync drift on the next sidebar change. |
| 2026-07-18 | proposed (F-M-poll-error-no-stop) | duck-challenger | useMessageStatus refetchInterval returned the poll interval when query.state.data was undefined (error state) — a permanent 4xx/5xx looped the failing GET /messages/:id every 2s forever with the spinner frozen on PENDING and no feedback. Fixed: return false when query.state.status==="error"; SandboxPage stops the spinner and shows result.statusError (×3 i18n) on isError. |
| 2026-07-18 | not-in-checklist (propose: B-H-missing-inject-on-concrete-param) | code-reviewer | SendTextMessageUseCase: last constructor param drainOutbox: DrainOutboxUseCase has no @inject decorator — tsyringe resolves it by reflect-metadata type token, which works only because DrainOutboxUseCase is a concrete @injectable class registered as singleton; fragile convention for infra injection in application layer (B-C10 adjacent). |
| 2026-07-18 | not-in-checklist (propose: B-M-dispatch-text-null-coalesce-vs-check) | code-reviewer | dispatchOutboxSend text case uses `message.text ?? ""` — passes an empty string to sendText, bypassing the CHECK constraint guarantee that text-type rows always have non-null text; risk is low because sendText is only reached for type="text" rows that the DB constraint enforces non-null text, but the fallback string creates a silent divergence between what the DB guarantees and what the code communicates. |
| 2026-07-18 | not-in-checklist (propose: B-M-drain-spec-text-only-enqueue) | code-reviewer | drain-outbox.use-case.spec.ts `enqueue` helper always sets `text: t-${key}` and never `type`/`payload` — the existing drain tests confirm text drain still works but do not test non-text replay via the test-level plumbing (the AC-6 rich-drain test creates directly via `outbox.create`, bypassing the helper, which is correct but the helper should be updated to support `type`/`payload` for future test reuse). |
| 2026-07-18 | not-in-checklist (propose: B-H-multi-handler-spec-table-gap) | code-auditor | message.controller + public-api.controller each gained 6 new rich-send handlers; iter1/iter2 spec tables covered only a subset (image/list, then audio/video/document), leaving sendPix (internal) and SendAudio/Video public DTOs unexercised — a handler that delegates to a shared private method still needs its type-dispatch exercised once. Fixed: table-driven it.each + explicit sendPix/audio/video cases across both controller specs and both dto specs. |
| 2026-07-18 | X-C3 (fixed) | migration-safety | outbox_message type/payload change was first shipped as an incremental migration folder, but repo convention (git history 3f6f046) squashes all migrations into a single regenerated _first baseline (pre-production dev-zero). Fixed: regenerated _first/migration.sql via prisma migrate diff --from-empty, removed the incremental folder, added the text-XOR-payload CHECK constraint + a schema.prisma doc-note (Prisma does not model CHECK). |
| 2026-07-28 | not-in-checklist (propose: B-H17 lazy-init gateway guard) | backend babysit (code-auditor) | BaileysWhatsAppGateway.setTyping first shipped with `await this.getManager()`, forcing a lazy Baileys dynamic import on a cosmetic presence call — inconsistent with the best-effort siblings isConnected/getCurrentQr/closeAll which guard `this.manager?`. Fixed: `return this.manager?.setTyping(...)`; no manager ⇒ no open socket ⇒ safe no-op without building the manager. |
| 2026-07-28 | SC-H4 (fixed) | backend babysit (code-auditor) | humanized-send-pacing (E1) had a root ROADMAP but no formal Task Spec in .claude/specs/ — the project gate expects `.claude/specs/<slug>.md` with AC↔code mapping. Fixed: wrote .claude/specs/humanized-send-pacing.md (E1 ACs + Decisions log D1–D3, E2–E6 stubbed), cross-referencing the root roadmap. |
| 2026-07-28 | not-in-checklist (propose: B-M-env-cross-field-error-no-message) | code-reviewer | env.ts superRefine prints only the field name on boot-fail ("Missing or invalid: TYPING_MIN_MS") without the cross-field diagnostic message, leaving operator with no hint that the problem is min > max (not a bad value in isolation). Consider printing the per-field messages alongside the keys. |
| 2026-07-28 | proposed: X-M4 cross-field env refine (fixed) | backend babysit (code-auditor+reviewer) | E2 added TYPING_MIN/MAX_MS and LONG_PAUSE_MIN/MAX_MS — first min/max env pairs; each validates in isolation but an inverted pair silently degrades the pacer. Fixed: added a `superRefine` enforcing min<=max on both pairs, AND made the boot-fail handler print `error.issues[].message` so the reason (not just the field name) shows. Reverses spec D5. |
| 2026-09-15 | not-in-checklist (propose: X-M-skill-doc-crud-scaffolding-drift) | code-reviewer | AC-11 synced patterns/frontend.md's dead-CRUD-scaffolding references (CrudRepository, useListPageController, ListPageLayout, DataTable...) after deleting the 56-file island, and commands/frontend.md was touched in the same diff (React 18→19 / Chakra v2→v3 line), but its own decision-tree section (lines 98/121/124/166) still points at the same now-deleted components — the exact drift class AC-11 exists to kill, one file over. commands/fullstack.md and commands/ui-design.md (untouched, out of diff) carry the identical drift. |
| 2026-09-15 | not-in-checklist (propose: SC-M-ac-grep-catches-comments) | code-reviewer | AC-2's literal verification command (grep for extendTheme/defineStyleConfig/ColorModeScript/etc across apps/web/src) returns 2 hits — AppProviders.tsx:28 and recipes.ts:3 — both explanatory comments referencing the old v2 API name for context, not real usage. Real AC-2 intent (no v2 theme APIs in use) is satisfied; the AC's own grep is not literally clean, which will trip a mechanical /finish-task gate running the same command verbatim. |
| 2026-09-15 | F-C21 (novo — promovido para hook + checklist) | code-auditor | Chakra v2 `colorScheme="brand"` sobreviveu ao sweep no CTA de `DevicesListPage`. A v3 remapeia a prop para o CSS nativo `color-scheme` (string livre), então passa por `tsc` E `eslint` enquanto o botão perde a paleta. Corrigido para `colorPalette`; regra adicionada a `post-edit-frontend.sh` e ao checklist, porque type-check nunca vai pegar essa classe. |
| 2026-09-15 | F-C2 (fixed) | code-auditor | `color: "#ffffff"` cru no `contentProps` do Tooltip do LanguageSelector → token `white`. |
| 2026-09-15 | F-H15-adjacent (fixed) | code-auditor | `aria-label="Close"` hardcoded no snippet `close-button`: todo modal anunciava "Close" em pt-BR/es. Rótulo passou a vir do call site (`AppModal` → `t("actions.close")`). |
| 2026-09-15 | not-in-checklist (propose: F-H-initial-focus-ref-null-on-open) | frontend babysit (teste sugerido pelo reviewer) | `initialFocusEl={() => ref.current}` num Dialog v3 não aplica foco: o ref ainda é null quando o zag resolve o callback no commit de abertura. Resolver o nó do DOM sob demanda. Só apareceu porque o teste foi escrito. |
| 2026-09-15 | SC-C1 | code-reviewer | refactor-chakra-v3-standardize: Decisions log claims F-C21 checklist entry, post-edit-frontend.sh F-C21 grep, and commands/{frontend,fullstack,ui-design}.md doc-sync were done, but those fixes sit unstaged in the worktree — `git diff --cached origin/develop` (what would actually be committed) still ships the old checklist/hook and stale commands/frontend.md scaffolding refs (useListPageController, ListPageLayout, EditableInfoGrid, DataTable, ProfileHeader, TagBadge, FadeIn/Stagger*, useServerListPage). |
| 2026-09-15 | SC-H1 (AC-11 partial) | code-reviewer | Staged patterns/frontend.md still references deleted files as live patterns (usePrefetchEntity, LinkEntityModal, useEntityActions/useEntityCreate backbone) at lines ~407/411/424 — the corrected prose exists only in the unstaged working tree copy of the same file. |
| 2026-09-15 | SC-C1 (fixed) | code-reviewer (finish-task Fase 5) | Gap de STAGING, não de código: `git add -A` rodou antes da babysit loop e as 21 correções seguintes (fix do `colorScheme`, regra F-C21 no hook, entrada no checklist, limpeza dos docs de skill) ficaram só na working tree. O índice — o que vira commit — teria ido sem nenhuma. Lição: re-stage e re-verifique contra o ÍNDICE depois de cada rodada de correção, nunca contra a working tree. |
| 2026-09-15 | X-H1-adjacent (fixed) | code-reviewer (finish-task Fase 5) | `apps/web/index.html` tinha script de boot forçando light em todo load (gravava `chakra-ui-color-mode`, chave que ninguém lê; setava `data-theme`, que o next-themes ignora). Pré-existente, mas sabotava o AC-5 desta spec — usuário dark levava flash de light em toda visita. Removido e verificado em runtime. |
| 2026-09-15 | SEC-C6 (fixed) | security-auditor (backend audit) | Outbound webhook sender fetched any customer-supplied URL — no scheme/host allowlist, redirects followed. Added `shared/util/ssrf-guard.ts` (resolve-at-send, ALL records public, `redirect: "manual"`). |
| 2026-09-15 | SEC-C7 (fixed) | security-auditor (backend audit) | `request-password-reset` logged the typed e-mail on the silent-success path; `email` was also missing from the pino redact list. |
| 2026-09-15 | SEC-H6 (fixed) | security-auditor (backend audit) | Avatar S3 key took its extension from the client filename (`evil.svg` with `image/png`) — now derived from the validated MIME, re-checked in the use case. |
| 2026-09-15 | B-C14 (fixed, new code) | code-auditor (backend audit) | `userRoutes.use(authMiddleware)` mounted the FACTORY: every `/users/*` request hung and the guard never ran (masked an account-takeover CRUD). Surface deleted (zero consumers); hook + `route-mount.spec.ts` now catch the class. |
| 2026-09-16 | B-H4-adjacent (fixed) | code-auditor (backend audit) | 137 of 186 `ErrorCodes` were cuidda leftovers never emitted here; `errors.json` had 169 keys (2 real codes untranslated). Pruned to 48 + `error-codes.spec.ts` locale-parity gate. |
| 2026-09-16 | SEC-H (proposed: process-exit-ownership) (fixed) | security-auditor (babysit) | Bugsnag's default `uncaughtException`/`unhandledRejection` listeners `process.exit(1)` right after delivery (verified in @bugsnag/node dist), racing the new staged teardown — a crash could exit before the WhatsApp sockets closed and the advisory lock was released. SDK process-level capture disabled; the lifecycle reports via an awaitable `errorReporter.notify` and owns the exit. |
| 2026-09-16 | not-in-checklist (propose: SEC-M-mime-trust-not-verify-doc) (fixed) | duck-challenger | `update-avatar.use-case.ts` / `upload.middleware.ts` derive the S3 ContentType/extension from the client-declared multipart Content-Type with no magic-byte check; AC-5-compliant (closes the originalname vector) but the comment's "validated" overclaimed content verification — comment now says trusted-not-verified + the downstream `nosniff` belt; deferral logged in the spec. |
| 2026-09-16 | not-in-checklist (propose: SEC-M-ssrf-no-private-target-override) (documented) | duck-challenger | `assertSafeOutboundUrl` has no override/allowlist path for a legitimate private-range webhook target (on-prem customer, local dev receiver); was an omission — now an explicit "Out (deferred)" entry in the spec + PR callout. |
| 2026-09-16 | SC-H2 (false alarm — branch base lagged origin/develop) | code-auditor (babysit) | 4 root docs looked like scope creep in `git diff --cached origin/develop`; they were tracked in the branch's old base and origin/develop had since deleted them (031871e). Fixed by moving the base (`reset --soft origin/develop`), not by unstaging. Lesson: fetch + compare the base BEFORE the first audit, or the auditor chases ghosts. |
| 2026-09-16 | SC-C1-adjacent (downgraded: High) | code-reviewer | web-rebuild-roadmap.md claims "34 rotas" 3x (main §2, Apêndice B header, baked into the spec's AC-3), but the actual route files enumerate 37 business routes (13 auth + 2 account + 9 devices + 7 messaging + 6 public-api) or 39 incl. the 2 health routes. |
| 2026-09-16 | SC-H1-adjacent (proposed: token-contract-dynamic-blind-spot) | code-auditor (audit) | `toaster.tsx` builds `status.\${config.palette}.{bg,border,solid,fg}` via template literals for `backgroundImage`/`borderColor`/`bg`/`color` — confirmed by probe that `tokenContract.spec.ts`'s `TOKEN_PROP`/`TOKEN_REF` regexes match zero of these 5 usages, so AC-5's "every token used in src exists" claim doesn't actually cover the one file explicitly named in Scope-In for the toast tokens (currently correct, but a future rename would break undetected). |
| 2026-09-16 | SC-H1-adjacent (proposed: token-contract-scope-excludes-own-package) | code-reviewer | `apps/web/src/app/theme/tokenContract.spec.ts`'s `SRC_ROOT` resolves to `apps/web/src` only, so its "every token literal used in src exists" check never scans `packages/theme/src/foundations/recipes.ts` (or any other theme-package file) — the exact file where hand-authored token-name strings (`bg: "bg.brand.solid"`, `boxShadow: "input-focus"`, ...) live and where the upcoming Fase 4 design-handoff pass will edit most. A typo there renders nothing and goes undetected by type-check, lint (packages/theme has no lint script), or this spec. |
| 2026-09-16 | not-in-checklist (propose: F-C-warm-default-palette-bypass) (fixed) | duck-challenger | `tokenContract.spec.ts` trusted Chakra's defaultConfig token registry, which ships `orange`/`yellow` palettes as known tokens — `bg="orange.500"` would have passed the no-warm-hue gate. Fixed: explicit warm-palette, named-color and `colorPalette` rejection independent of registry membership. |
| 2026-09-16 | not-in-checklist (propose: F-M-decorative-token-parity-blind-spot) (fixed) | duck-challenger | the base/_dark presence check could not tell an intentionally identical decorative token from a forgotten dark value. Fixed: closed `SAME_IN_BOTH_MODES` allowlist + assertion that no other semantic token has base === _dark. |
| 2026-09-16 | not-in-checklist (propose: token-contract-warm-scan-excludes-recipes) | code-reviewer (finish-task gate) | `tokenContract.spec.ts`'s warm-hue test (`isWarm`) only scans `colors`/`shadows`/`semanticTokens` — it never imports or scans `recipes.ts`, whose `buttonRecipe` hand-authors 5 literal `rgba(...)` box-shadow strings (currently green/red, non-warm). Test 4's raw-CSS skip (`/[\s(#]/`) also lets these through, and the mechanical hook (`post-edit-frontend.sh`) is scoped to `apps/web/src/**` only, never `packages/theme/src/**`. A future design-handoff editing a recipe's box-shadow to an orange/amber rgba would ship undetected by every guard (contract spec, hook, type-check, lint). |
| 2026-09-16 | not-in-checklist (propose: F-C-token-swap-hue-mismatch) (fixed) | duck-challenger | ForgotPasswordPage "link sent" banner moved from raw `brand.*` to `status.success.*` (Chakra default green, another hue) — undeclared visual change in a "keep the look" phase; now `bg.accent.subtle`/`text.brand`/`border.accent` (same emerald ramp), border-shade delta logged in the Fase 6 spec. |
| 2026-09-16 | not-in-checklist (propose: F-H-optimistic-rollback-snapshot-clobber) | code-reviewer | `useDeleteDevice`'s `onError` restores the full pre-mutation `context.previous` list snapshot; a second concurrent delete (single `ConfirmDialog` closes instantly on confirm, so a fast second delete-confirm is reachable) has its own optimistic removal overwritten when the first mutation's rollback fires, transiently resurrecting an already-deleted/in-flight device in the list until `onSettled`'s invalidation refetch corrects it. Matches `frontend.md`'s canonical optimistic-delete snippet verbatim, so the same latent race exists wherever that snippet is copied.
| 2026-09-16 | not-in-checklist (propose: F-H-optimistic-rollback-snapshot-clobber) (fixed) | code-reviewer (iteration 2) | Verified: `useDeleteDevice`'s `onError` now merges only the failed device back via `restoreDevice` (position-preserving, no-op if already present/unknown) instead of overwriting the whole list snapshot; traced through a concurrent double-failure case — both items end up restored, only a cosmetic order swap remains (self-heals via the unchanged `onSettled` invalidation). `frontend.md`'s canonical snippet updated to match.
| 2026-09-16 | SC-H1 (fixed, iteration 2) | code-reviewer | `ProfileAvatarCard.tsx:96` keeps `bg="brand.500"` (raw palette) on the Avatar fallback background; AC-6 of `web-rebuild-fase-9-profile-api` explicitly requires "nenhuma cor crua da paleta no card de avatar" and no Decisions-log entry documents the exception — the closest semantic token (`bg.brand.solid` = brand.600 in light mode) would actually change the light-mode shade, so the gap needs an explicit accepted-exception note (or a new semantic token), not a silent carry-over. Same raw pattern pre-exists in `SidebarUserMenu.tsx`/`MobileHeader.tsx` (out of this diff). |
| 2026-09-16 | SEC-H4 (proposed: refresh-clear-on-error-logout-csrf) (fixed) | security-auditor | `auth.controller.ts` `refresh()` now calls `clearAuthCookies(res)` when no refresh token is present; that branch is reachable via a plain cross-site `<form>` POST to `/api/auth/refresh` (SameSite strips the victim's cookies from the outgoing request, but the browser still applies the response's `Set-Cookie` clears) — a zero-JS forced logout of any signed-in user. Fixed: clears only when the request carried `pombo_at`, `pombo_rt` or `pombo_csrf`. |
| 2026-09-16 | E-H4 (downgraded: Medium) | code-reviewer | `device-crud.spec.ts` bundles create+webhook-edit+delete+search (4 flows) and `sandbox-send.spec.ts` bundles text/media/group/empty/negative sends in one file each, citing a parallel-agent file-ownership trade-off in the file's own docstring; each `describe` is independently self-contained (own setup/teardown), so downgraded from the checklist's High — recommend a follow-up split to match `patterns/e2e.md`'s one-flow-per-file convention before it becomes the copied template.
| 2026-09-16 | proposed (SEC-H9): IP rate-limit trust-anchor spoofable via open origin firewall | security-auditor | `authRateLimit` (and every other IP-keyed limiter — global/user/public/api-token) trusts `req.ip` under `trust proxy: 1`; Caddy sets `X-Forwarded-For` = Cloudflare's `CF-Connecting-IP` verbatim (`infra/app/Caddyfile`), but the APP host's `ufw` currently accepts 80/443 from any source, not just Cloudflare's ranges (`.claude/knowledge/devops.md` already tracks this: "Origin firewall: accept 80/443 only from Cloudflare IP ranges ... Gate before real traffic"). A direct hit to the origin IP can set an arbitrary `CF-Connecting-IP` per request, making the auth limiter's key attacker-chosen — the 10-req/15-min budget can be bypassed from a single machine with no botnet, nullifying credential-stuffing/brute-force throttling on sign-in, password-reset and refresh. Not introduced by this branch (infra state, unrelated to the web-rebuild diff) — re-surfaced during the `/security` pass requested by `web-rebuild-fase-10-gate-final` AC-5 because it directly undermines the control being validated. Fix: restrict the origin firewall to Cloudflare IP ranges (or Authenticated Origin Pulls) before trusting IP-based rate limiting in production.
| 2026-09-16 | none (Critical/High) — clean gate-final review | code-reviewer | Fase 10 gate-final (06b37d6/d473274/2008863/a99e2af/cbb1351/ffdb68a/96df01f): signal passthrough traced through axios 1.18.1's CanceledError/timeout-vs-cancel distinction (both XHR and fetch adapters) with no conflation risk; useFormState.setField's new latestRef has no bypass writer; i18n orphan sweep spot-checked across auth/common/settings.json with no live key removed; setup-env.ts fallback confirmed scoped to unit tests only (no real-DB integration path in apps/api/vitest.config.ts). No Critical/High found. |
