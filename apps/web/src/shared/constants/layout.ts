/** Desktop sidebar widths — shared by `AppShell`, its mobile drawer and the
 *  shell loading skeleton, so the placeholder never jumps when the app lands. */
export const SIDEBAR_WIDTH = {
  collapsed: "68px",
  expanded: "248px",
} as const;
